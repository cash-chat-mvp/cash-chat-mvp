# CC-311 S4 — Evolution Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 확률형 진화 도메인 추가 — `evolutionExp`를 소비해 서버 보안 난수로 1회 결정·저장되는 레벨업(Lv1→Lv5)을 제공한다.

**Architecture:** 진화 상태(level/exp/failStack)는 기존 `UserWallet`에 보유(사용자당 단일 진화 라인). 신규 `domain/evolution` 도메인이 시도 기록(`evolution_attempt`)과 멱등·RNG·정산을 담당. EXP 차감·레벨·failStack 변경은 지갑 행 락 아래 단일 트랜잭션. 멱등키는 `Idempotency-Key` 헤더(=evolutionAttemptId), 재요청 시 저장된 결과 반환(I14).

**Tech Stack:** Kotlin 1.9.25, Spring Boot 3.5.11, JPA(`ddl-auto: validate`)+Flyway, Kotest FunSpec + TestContainers(MySQL) + mockito-kotlin, `@WebMvcTest` 웹 슬라이스.

## Global Constraints

- **로그인/인증 코드 절대 불변**: `domain/auth`, JWT 필터, `SecurityConfig` 미수정. 모든 진화 상태는 userId 기준.
- **광고 SSV 검증 로직 절대 불변**: `domain/ad`, nonce/quota/콜백 미수정.
- **내부 원가/마진 수치(`NANO_COST_PT=0.68`, `SHARED_POOL_MARGIN_PT=0.32`, `ENERGY_BACKING_PT=2.00`) 소스·커밋 문서에 하드코딩 금지.** 진화 수치(필요 EXP·성공률·보존율)는 cost/margin 비공개 대상이 **아니므로** `EvolutionProperties` 기본값으로 커밋 가능.
- `ddl-auto: validate` → 엔티티와 Flyway SQL은 **정확히 일치**해야 한다(컬럼명·타입). dev=H2(MySQL 모드), prod=MySQL 8.
- 멱등성: 잔액/상태 변경은 고유 키. 지갑 행 락(`PESSIMISTIC_WRITE`) → 멱등 조회 → 변경 → 기록, DB unique 제약이 최종 방어선(기존 `EnergyService`/`ChatRewardSettlementService` 패턴 재사용).
- **확정 결정(유저)**: ① failStack 공식 = 선형 `finalRate = min(1.0, baseRate + failStack × failStackBonus)`, `failStackBonus` 기본 0.10. ② 실패 시 EXP = 레벨별 보존율(Lv1→2,Lv2→3: 0.0 / Lv3→4: 0.20 / Lv4→5: 0.30). ③ 멱등키 = `Idempotency-Key` 헤더. ④ 최대레벨(Lv5) 도달 후 시도 = `EVOLUTION_MAX_LEVEL`/422.
- 진화 수치(§8.2, §12, Confluence CC-283): Lv1→2(req 30, 0.80, keep 0.0), Lv2→3(req 100, 0.60, keep 0.0), Lv3→4(req 300, 0.35, keep 0.20), Lv4→5(req 1000, 0.15, keep 0.30). maxLevel=5, policyVersion=1.

## 불변식 (Confluence §13)

- **I6**: 현재 진화 시도는 `evolutionExp`만 사용(cashablePt 미사용).
- **I14**: 진화 결과는 서버에서 한 번만 결정하고 변경 불가(동일 evolutionAttemptId 재요청 시 저장된 결과 반환).
- 성공: `level+1, exp=0, failStack=0`. 실패: `level 유지, exp = floor(exp × keepRatio), failStack+1`.
- `evolutionExp ≥ requiredExp` 일 때만 시도 가능. `expectedLevel ≠ 현재 level` → `EVOLUTION_LEVEL_MISMATCH`/422.

---

## File Structure

- `domain/economy/properties/EvolutionProperties.kt` — 신규 `@ConfigurationProperties("app.evolution")`.
- `domain/economy/persistence/entity/UserWallet.kt` — 진화 mutator 2개 추가(수정).
- `domain/economy/persistence/entity/WalletTxType.kt` — `EXP_CONSUMED` 추가(수정).
- `domain/evolution/persistence/entity/EvolutionAttempt.kt`, `EvolutionResult.kt` — 신규.
- `domain/evolution/persistence/repository/EvolutionAttemptRepository.kt` — 신규.
- `domain/evolution/service/EvolutionRandom.kt`(+`SecureRandomEvolutionRandom`), `EvolutionService.kt` — 신규.
- `domain/evolution/exception/*.kt` — 신규(LevelMismatch/InsufficientExp/MaxLevel/AttemptNotFound/IdempotencyKeyRequired).
- `domain/evolution/web/controller/EvolutionController.kt`, `web/request/EvolutionAttemptRequest.kt`, `web/response/EvolutionMeResponse.kt`, `web/response/EvolutionAttemptResponse.kt`, `web/exception/EvolutionExceptionHandler.kt` — 신규.
- `src/main/resources/db/migration/V8__evolution_attempt.sql` — 신규.
- `EvolutionProperties` 등록은 기존 `EconomyProperties` 등록 방식(`@EnableConfigurationProperties` 또는 `@ConfigurationPropertiesScan` 위치)과 동일하게 따른다.

---

### Task 1: EvolutionProperties + UserWallet mutators + WalletTxType

**Files:**
- Create: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/economy/properties/EvolutionProperties.kt`
- Modify: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/economy/persistence/entity/UserWallet.kt`
- Modify: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/economy/persistence/entity/WalletTxType.kt`
- Test: `apps/backend/src/test/kotlin/com/wnl/cashchat/api/domain/economy/EvolutionPropertiesTest.kt`
- Test: `apps/backend/src/test/kotlin/com/wnl/cashchat/api/domain/economy/UserWalletEvolutionTest.kt`

**Interfaces:**
- Produces:
  - `EvolutionProperties(maxLevel: Int=5, policyVersion: Int=1, failStackBonus: Double=0.10, levels: List<LevelPolicy>)` with `data class LevelPolicy(level: Int, requiredExp: Long, baseSuccessRate: Double, failKeepRatio: Double)` and `fun policyFor(level: Int): LevelPolicy?`.
  - `UserWallet.applyEvolutionSuccess()` → level+1, exp=0, failStack=0.
  - `UserWallet.applyEvolutionFailure(keepRatio: Double)` → level 유지, exp = floor(exp×keepRatio), failStack+1.
  - `WalletTxType.EXP_CONSUMED`.

- [ ] **Step 1: Write the failing tests**

`EvolutionPropertiesTest.kt`:
```kotlin
package com.wnl.cashchat.api.domain.economy

import com.wnl.cashchat.api.domain.economy.properties.EvolutionProperties
import io.kotest.core.spec.style.FunSpec
import io.kotest.matchers.shouldBe

class EvolutionPropertiesTest : FunSpec({
    val props = EvolutionProperties()

    test("defaults match Confluence CC-283 numbers") {
        props.maxLevel shouldBe 5
        props.policyVersion shouldBe 1
        props.failStackBonus shouldBe 0.10
        props.policyFor(1) shouldBe EvolutionProperties.LevelPolicy(1, 30, 0.80, 0.0)
        props.policyFor(2) shouldBe EvolutionProperties.LevelPolicy(2, 100, 0.60, 0.0)
        props.policyFor(3) shouldBe EvolutionProperties.LevelPolicy(3, 300, 0.35, 0.20)
        props.policyFor(4) shouldBe EvolutionProperties.LevelPolicy(4, 1000, 0.15, 0.30)
    }

    test("policyFor returns null at and beyond max level") {
        props.policyFor(5) shouldBe null
    }
})
```

`UserWalletEvolutionTest.kt`:
```kotlin
package com.wnl.cashchat.api.domain.economy

import com.wnl.cashchat.api.domain.economy.persistence.entity.UserWallet
import com.wnl.cashchat.api.domain.user.persistence.entity.User
import io.kotest.core.spec.style.FunSpec
import io.kotest.matchers.shouldBe
import io.mockk.mockk

class UserWalletEvolutionTest : FunSpec({
    fun wallet(): UserWallet = UserWallet(user = mockk<User>(relaxed = true))

    test("success raises level and resets exp and failStack") {
        val w = wallet()
        w.addExp(120); repeat(3) { w.applyEvolutionFailure(0.0) } // failStack=3, exp=0
        w.addExp(120)
        w.applyEvolutionSuccess()
        w.evolutionLevel shouldBe 2
        w.evolutionExp shouldBe 0
        w.evolutionFailStack shouldBe 0
    }

    test("failure keeps level, floors exp by ratio, increments failStack") {
        val w = wallet()
        w.addExp(301)
        w.applyEvolutionFailure(0.20)
        w.evolutionLevel shouldBe 1
        w.evolutionExp shouldBe 60   // floor(301 * 0.20) = 60
        w.evolutionFailStack shouldBe 1
    }

    test("failure with zero keep ratio resets exp to zero") {
        val w = wallet()
        w.addExp(100)
        w.applyEvolutionFailure(0.0)
        w.evolutionExp shouldBe 0
        w.evolutionFailStack shouldBe 1
    }
})
```
구현자 노트: 기존 테스트가 io.mockk 대신 mockito-kotlin을 쓰면 `User` 인스턴스 생성 방식을 기존 economy 단위 테스트 컨벤션에 맞춘다(목적은 FK 무관한 in-memory UserWallet).

- [ ] **Step 2: Run tests to verify they fail**

Run (foreground, from `apps/backend`): `./gradlew test --tests "com.wnl.cashchat.api.domain.economy.EvolutionPropertiesTest" --tests "com.wnl.cashchat.api.domain.economy.UserWalletEvolutionTest"`
Expected: FAIL — `EvolutionProperties`/mutators 미존재(compile error).

- [ ] **Step 3: Implement EvolutionProperties**

`EvolutionProperties.kt`:
```kotlin
package com.wnl.cashchat.api.domain.economy.properties

import jakarta.validation.constraints.PositiveOrZero
import org.springframework.boot.context.properties.ConfigurationProperties
import org.springframework.validation.annotation.Validated

@Validated
@ConfigurationProperties(prefix = "app.evolution")
data class EvolutionProperties(
    val maxLevel: Int = 5,
    @field:PositiveOrZero val policyVersion: Int = 1,
    @field:PositiveOrZero val failStackBonus: Double = 0.10,
    val levels: List<LevelPolicy> = DEFAULT_LEVELS,
) {
    data class LevelPolicy(
        val level: Int,
        val requiredExp: Long,
        val baseSuccessRate: Double,
        val failKeepRatio: Double,
    )

    fun policyFor(level: Int): LevelPolicy? = levels.firstOrNull { it.level == level }

    companion object {
        val DEFAULT_LEVELS = listOf(
            LevelPolicy(1, 30, 0.80, 0.0),
            LevelPolicy(2, 100, 0.60, 0.0),
            LevelPolicy(3, 300, 0.35, 0.20),
            LevelPolicy(4, 1000, 0.15, 0.30),
        )
    }
}
```

- [ ] **Step 4: Register the properties + add mutators + EXP_CONSUMED**

먼저 기존 `EconomyProperties` 등록 방식을 확인한다: `grep -rn "EconomyProperties" apps/backend/src/main` 로 `@EnableConfigurationProperties` 또는 `@ConfigurationPropertiesScan` 위치를 찾아, **동일한 곳에 `EvolutionProperties`를 같은 방식으로 등록**한다(예: `@EnableConfigurationProperties(EconomyProperties::class, EvolutionProperties::class)`).

`WalletTxType.kt` — `EXP_GRANTED,` 뒤에 `EXP_CONSUMED` 추가:
```kotlin
package com.wnl.cashchat.api.domain.economy.persistence.entity

enum class WalletTxType {
    ENERGY_GRANTED, ENERGY_RESERVED, ENERGY_CONSUMED, ENERGY_REFUNDED, ENERGY_EXPIRED,
    POINT_PENDING_GRANTED, POINT_CONFIRMED, EXP_GRANTED, EXP_CONSUMED,
}
```

`UserWallet.kt` — `addExp` 아래에 추가(`require`로 불변 보호):
```kotlin
    fun applyEvolutionSuccess() {
        evolutionLevel += 1
        evolutionExp = 0
        evolutionFailStack = 0
    }
    fun applyEvolutionFailure(keepRatio: Double) {
        require(keepRatio in 0.0..1.0) { "keepRatio must be within [0,1]" }
        evolutionExp = Math.floor(evolutionExp * keepRatio).toLong()
        evolutionFailStack += 1
    }
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `./gradlew test --tests "com.wnl.cashchat.api.domain.economy.EvolutionPropertiesTest" --tests "com.wnl.cashchat.api.domain.economy.UserWalletEvolutionTest"`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(evolution): add EvolutionProperties, UserWallet evolution mutators, EXP_CONSUMED"
```

---

### Task 2: EvolutionAttempt entity + repository + V8 migration

**Files:**
- Create: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/evolution/persistence/entity/EvolutionResult.kt`
- Create: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/evolution/persistence/entity/EvolutionAttempt.kt`
- Create: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/evolution/persistence/repository/EvolutionAttemptRepository.kt`
- Create: `apps/backend/src/main/resources/db/migration/V8__evolution_attempt.sql`
- Test: `apps/backend/src/test/kotlin/com/wnl/cashchat/api/domain/evolution/EvolutionAttemptRepositoryTest.kt`

**Interfaces:**
- Consumes: `BaseEntity`(created_at/updated_at 제공), `users` 테이블 FK.
- Produces:
  - `enum class EvolutionResult { SUCCESS, FAIL }`.
  - `EvolutionAttempt(userId, attemptKey, levelBefore, levelAfter, requiredExp, baseSuccessRate, failStackBefore, finalSuccessRate, rollValue, result, expAfter, failStackAfter, policyVersion)` — 모든 필드 생성자 주입, `id` 자동.
  - `EvolutionAttemptRepository.findByUserIdAndAttemptKey(userId, attemptKey): EvolutionAttempt?`, `findByIdAndUserId(id, userId): EvolutionAttempt?`.

- [ ] **Step 1: Write the failing test**

`EvolutionAttemptRepositoryTest.kt` (기존 economy 통합 테스트 컨테이너 관례 따름 — `MySQLContainer("mysql:8.4.0")` + `@DynamicPropertySource`; 기존 `EnergyServiceIntegrationTest`의 셋업·User 시드를 그대로 모방하되 본 파일에 맞게 작성):
```kotlin
package com.wnl.cashchat.api.domain.evolution

import com.wnl.cashchat.api.domain.evolution.persistence.entity.EvolutionAttempt
import com.wnl.cashchat.api.domain.evolution.persistence.entity.EvolutionResult
import com.wnl.cashchat.api.domain.evolution.persistence.repository.EvolutionAttemptRepository
import com.wnl.cashchat.api.domain.user.persistence.repository.UserRepository
import io.kotest.assertions.throwables.shouldThrow
import io.kotest.core.spec.style.FunSpec
import io.kotest.matchers.nulls.shouldNotBeNull
import io.kotest.matchers.shouldBe
import org.springframework.boot.test.context.SpringBootTest
import org.springframework.dao.DataIntegrityViolationException
// + 기존 통합 테스트와 동일한 TestContainers/@DynamicPropertySource 보일러플레이트

@SpringBootTest
class EvolutionAttemptRepositoryTest(
    private val repository: EvolutionAttemptRepository,
    private val userRepository: UserRepository,
) : FunSpec({
    // beforeTest: repository.deleteAll(); userRepository.deleteAll() (FK 안전 순서)

    test("save and find by userId + attemptKey") {
        val user = userRepository.save(/* 기존 테스트의 User 팩토리 */)
        repository.save(attempt(user.id, "key-1"))
        val found = repository.findByUserIdAndAttemptKey(user.id, "key-1")
        found.shouldNotBeNull()
        found.result shouldBe EvolutionResult.FAIL
    }

    test("unique(user_id, attempt_key) rejects duplicate") {
        val user = userRepository.save(/* User 팩토리 */)
        repository.saveAndFlush(attempt(user.id, "dup"))
        shouldThrow<DataIntegrityViolationException> {
            repository.saveAndFlush(attempt(user.id, "dup"))
        }
    }
})

private fun attempt(userId: Long, key: String) = EvolutionAttempt(
    userId = userId, attemptKey = key, levelBefore = 1, levelAfter = 1,
    requiredExp = 30, baseSuccessRate = 0.80, failStackBefore = 0,
    finalSuccessRate = 0.80, rollValue = 0.99, result = EvolutionResult.FAIL,
    expAfter = 0, failStackAfter = 1, policyVersion = 1,
)
```
구현자 노트: 기존 economy 통합 테스트(`EnergyServiceIntegrationTest`)의 TestContainers 셋업·User 시드 헬퍼를 그대로 본받아 컴파일 가능한 형태로 완성한다.

- [ ] **Step 2: Run test to verify it fails**

Run: `./gradlew test --tests "com.wnl.cashchat.api.domain.evolution.EvolutionAttemptRepositoryTest"`
Expected: FAIL — 엔티티/리포지토리/테이블 미존재.

- [ ] **Step 3: Implement entity + enum + repository**

`EvolutionResult.kt`:
```kotlin
package com.wnl.cashchat.api.domain.evolution.persistence.entity

enum class EvolutionResult { SUCCESS, FAIL }
```

`EvolutionAttempt.kt`:
```kotlin
package com.wnl.cashchat.api.domain.evolution.persistence.entity

import com.wnl.cashchat.api.common.entity.BaseEntity
import jakarta.persistence.*

@Entity
@Table(
    name = "evolution_attempt",
    uniqueConstraints = [UniqueConstraint(
        name = "uq_evolution_attempt_user_key",
        columnNames = ["user_id", "attempt_key"],
    )],
)
class EvolutionAttempt(
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY) val id: Long = 0,
    @Column(name = "user_id", nullable = false) val userId: Long,
    @Column(name = "attempt_key", nullable = false) val attemptKey: String,
    @Column(name = "level_before", nullable = false) val levelBefore: Int,
    @Column(name = "level_after", nullable = false) val levelAfter: Int,
    @Column(name = "required_exp", nullable = false) val requiredExp: Long,
    @Column(name = "base_success_rate", nullable = false) val baseSuccessRate: Double,
    @Column(name = "fail_stack_before", nullable = false) val failStackBefore: Int,
    @Column(name = "final_success_rate", nullable = false) val finalSuccessRate: Double,
    @Column(name = "roll_value", nullable = false) val rollValue: Double,
    @Enumerated(EnumType.STRING) @Column(name = "result", nullable = false, length = 20)
    val result: EvolutionResult,
    @Column(name = "exp_after", nullable = false) val expAfter: Long,
    @Column(name = "fail_stack_after", nullable = false) val failStackAfter: Int,
    @Column(name = "policy_version", nullable = false) val policyVersion: Int,
) : BaseEntity()
```

`EvolutionAttemptRepository.kt`:
```kotlin
package com.wnl.cashchat.api.domain.evolution.persistence.repository

import com.wnl.cashchat.api.domain.evolution.persistence.entity.EvolutionAttempt
import org.springframework.data.jpa.repository.JpaRepository

interface EvolutionAttemptRepository : JpaRepository<EvolutionAttempt, Long> {
    fun findByUserIdAndAttemptKey(userId: Long, attemptKey: String): EvolutionAttempt?
    fun findByIdAndUserId(id: Long, userId: Long): EvolutionAttempt?
}
```

- [ ] **Step 4: Implement V8 migration (must match entity exactly)**

`V8__evolution_attempt.sql`:
```sql
-- V8: 확률형 진화 시도 기록

CREATE TABLE evolution_attempt (
    id                 BIGINT       NOT NULL AUTO_INCREMENT,
    user_id            BIGINT       NOT NULL,
    attempt_key        VARCHAR(255) NOT NULL,
    level_before       INT          NOT NULL,
    level_after        INT          NOT NULL,
    required_exp       BIGINT       NOT NULL,
    base_success_rate  DOUBLE       NOT NULL,
    fail_stack_before  INT          NOT NULL,
    final_success_rate DOUBLE       NOT NULL,
    roll_value         DOUBLE       NOT NULL,
    result             VARCHAR(20)  NOT NULL,
    exp_after          BIGINT       NOT NULL,
    fail_stack_after   INT          NOT NULL,
    policy_version     INT          NOT NULL,
    created_at         TIMESTAMP(6) NOT NULL,
    updated_at         TIMESTAMP(6) NOT NULL,
    PRIMARY KEY (id),
    CONSTRAINT uq_evolution_attempt_user_key UNIQUE (user_id, attempt_key),
    CONSTRAINT fk_evolution_attempt_user FOREIGN KEY (user_id) REFERENCES users (id)
);
```

- [ ] **Step 5: Run test to verify it passes**

Run: `./gradlew test --tests "com.wnl.cashchat.api.domain.evolution.EvolutionAttemptRepositoryTest"`
Expected: PASS (스키마 validate 통과 + 저장/조회/unique).

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(evolution): add EvolutionAttempt entity, repository, V8 migration"
```

---

### Task 3: EvolutionRandom + EvolutionService (attempt/me/findAttempt) + exceptions

**Files:**
- Create: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/evolution/service/EvolutionRandom.kt`
- Create: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/evolution/service/EvolutionService.kt` (`EvolutionMe` DTO 포함)
- Create: exceptions under `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/evolution/exception/`:
  `EvolutionLevelMismatchException.kt`, `EvolutionInsufficientExpException.kt`, `EvolutionMaxLevelException.kt`, `EvolutionAttemptNotFoundException.kt`, `EvolutionIdempotencyKeyRequiredException.kt`.
- Test: `apps/backend/src/test/kotlin/com/wnl/cashchat/api/domain/evolution/EvolutionServiceTest.kt` (통합, TestContainers).

**Interfaces:**
- Consumes: `WalletService.ensureForUpdate(userId)`/`snapshot(userId)`, `walletLedgerRepository.findByIdempotencyKey/save`(EXP_CONSUMED 기록), `EvolutionProperties`, `EconomyProperties.evolutionEnabled`, `FeatureDisabledException`(economy), `EvolutionAttemptRepository`.
- Produces:
  - `interface EvolutionRandom { fun roll(): Double }` (반환 [0.0, 1.0)) + `@Component class SecureRandomEvolutionRandom : EvolutionRandom`.
  - `EvolutionService.attempt(userId: Long, attemptKey: String, expectedLevel: Int): EvolutionAttempt` — `@Transactional`. 성공 판정 `roll() < finalSuccessRate`.
  - `EvolutionService.me(userId: Long): EvolutionMe` — `@Transactional(readOnly=true)`.
  - `EvolutionService.findAttempt(userId: Long, attemptId: Long): EvolutionAttempt` — `@Transactional(readOnly=true)`, 소유자 불일치/없음 → `EvolutionAttemptNotFoundException`.
  - `data class EvolutionMe(level, exp, failStack, maxLevel, requiredExp: Long?, baseSuccessRate: Double?, finalSuccessRate: Double?, canAttempt: Boolean)`.

- [ ] **Step 1: Write the failing tests**

`EvolutionServiceTest.kt` — 핵심 시나리오. `EvolutionRandom`은 고정값 fake 주입(테스트 빈 교체)하여 결정적으로 검증. 지갑 EXP/level/failStack은 트랜잭션 내 mutator 또는 테스트 헬퍼로 시드. 기존 통합 테스트 컨벤션(TestContainers, User 시드, beforeTest deleteAll FK 안전 순서)을 따른다.
```kotlin
package com.wnl.cashchat.api.domain.evolution

import com.wnl.cashchat.api.domain.economy.exception.FeatureDisabledException
import com.wnl.cashchat.api.domain.evolution.exception.*
import com.wnl.cashchat.api.domain.evolution.persistence.entity.EvolutionResult
import com.wnl.cashchat.api.domain.evolution.service.EvolutionService
import io.kotest.assertions.throwables.shouldThrow
import io.kotest.core.spec.style.FunSpec
import io.kotest.matchers.shouldBe
// + TestContainers 보일러플레이트, @SpringBootTest, EvolutionRandom 테스트 빈 주입

class EvolutionServiceTest : FunSpec({
    // 헬퍼: setWallet(userId, level, exp, failStack) — 지갑 행 생성 후 상태 세팅
    // fakeRandom.value 를 바꿔가며 roll() 제어

    test("success at Lv1 when roll below rate raises level and resets exp/failStack") {
        // userId 시드 level=1, exp=30, fakeRandom.value=0.5 (< 0.80)
        val r = service.attempt(userId, "k-succ", expectedLevel = 1)
        r.result shouldBe EvolutionResult.SUCCESS
        r.levelAfter shouldBe 2
        r.expAfter shouldBe 0
        r.failStackAfter shouldBe 0
    }

    test("failure at Lv1 keeps level, exp resets (keep 0.0), failStack increments") {
        // level=1, exp=40, fakeRandom.value=0.99 (>= 0.80)
        val r = service.attempt(userId, "k-fail", expectedLevel = 1)
        r.result shouldBe EvolutionResult.FAIL
        r.levelAfter shouldBe 1
        r.expAfter shouldBe 0
        r.failStackAfter shouldBe 1
    }

    test("I14: same attemptKey returns stored result without re-rolling") {
        // level=1, exp=30, fakeRandom.value=0.5 → 첫 시도 SUCCESS
        val first = service.attempt(userId, "k-idem", 1)
        // fakeRandom.value=0.99 로 바꿔도 동일 key 재요청은 저장 결과 반환
        val second = service.attempt(userId, "k-idem", expectedLevel = 1)
        second.id shouldBe first.id
        second.result shouldBe EvolutionResult.SUCCESS
        // 지갑이 두 번 변하지 않음(level=2, exp=0)
    }

    test("failStack raises finalSuccessRate linearly (bonus 0.10)") {
        // 시드: level=3, failStack=2, exp=300. base 0.35 → final 0.55. fakeRandom.value=0.50 (<0.55) → SUCCESS
        val r = service.attempt(userId, "k-stack", expectedLevel = 3)
        r.baseSuccessRate shouldBe 0.35
        r.finalSuccessRate shouldBe 0.55
        r.result shouldBe EvolutionResult.SUCCESS
    }

    test("expectedLevel mismatch throws") {
        // 지갑 level=1
        shouldThrow<EvolutionLevelMismatchException> { service.attempt(userId, "k-mm", expectedLevel = 2) }
    }

    test("insufficient exp throws") {
        // level=1, exp=10 < 30
        shouldThrow<EvolutionInsufficientExpException> { service.attempt(userId, "k-exp", expectedLevel = 1) }
    }

    test("max level attempt throws") {
        // level=5
        shouldThrow<EvolutionMaxLevelException> { service.attempt(userId, "k-max", expectedLevel = 5) }
    }

    test("findAttempt rejects other user's attempt") {
        val a = service.attempt(userId, "k-own", 1) // exp 충분 시드
        shouldThrow<EvolutionAttemptNotFoundException> { service.findAttempt(otherUserId, a.id) }
    }

    test("attempt throws FeatureDisabled when evolution disabled") {
        // evolutionEnabled=false 환경(별도 컨텍스트/프로퍼티) — 구현자가 가장 간단한 방법 선택
        shouldThrow<FeatureDisabledException> { disabledService.attempt(userId, "k-off", 1) }
    }
})
```
구현자 노트: `EvolutionRandom`을 테스트 빈으로 교체(`@TestConfiguration` 또는 `@MockBean`)하여 `roll()` 반환을 제어. 지갑 시드는 `WalletService.ensureForUpdate` + 트랜잭션 내 mutator 또는 직접 리포지토리 헬퍼로 설정.

- [ ] **Step 2: Run tests to verify they fail**

Run: `./gradlew test --tests "com.wnl.cashchat.api.domain.evolution.EvolutionServiceTest"`
Expected: FAIL — 서비스/예외 미존재.

- [ ] **Step 3: Implement EvolutionRandom + exceptions**

`EvolutionRandom.kt`:
```kotlin
package com.wnl.cashchat.api.domain.evolution.service

import org.springframework.stereotype.Component
import java.security.SecureRandom

interface EvolutionRandom {
    /** @return [0.0, 1.0) 균등 분포 보안 난수 */
    fun roll(): Double
}

@Component
class SecureRandomEvolutionRandom : EvolutionRandom {
    private val random = SecureRandom()
    override fun roll(): Double = random.nextDouble()
}
```

Exceptions (각 파일):
```kotlin
package com.wnl.cashchat.api.domain.evolution.exception
class EvolutionLevelMismatchException(val expected: Int, val actual: Int) :
    RuntimeException("Expected level $expected but current is $actual")
```
```kotlin
package com.wnl.cashchat.api.domain.evolution.exception
class EvolutionInsufficientExpException(val required: Long, val current: Long) :
    RuntimeException("Need $required exp but have $current")
```
```kotlin
package com.wnl.cashchat.api.domain.evolution.exception
class EvolutionMaxLevelException(val level: Int) :
    RuntimeException("Already at max level $level")
```
```kotlin
package com.wnl.cashchat.api.domain.evolution.exception
class EvolutionAttemptNotFoundException(val attemptId: Long) :
    RuntimeException("Evolution attempt $attemptId not found")
```
```kotlin
package com.wnl.cashchat.api.domain.evolution.exception
class EvolutionIdempotencyKeyRequiredException :
    RuntimeException("Idempotency-Key header is required")
```

- [ ] **Step 4: Implement EvolutionService**

`EvolutionService.kt`:
```kotlin
package com.wnl.cashchat.api.domain.evolution.service

import com.wnl.cashchat.api.domain.economy.exception.FeatureDisabledException
import com.wnl.cashchat.api.domain.economy.persistence.entity.WalletLedger
import com.wnl.cashchat.api.domain.economy.persistence.entity.WalletTxType
import com.wnl.cashchat.api.domain.economy.persistence.repository.WalletLedgerRepository
import com.wnl.cashchat.api.domain.economy.properties.EconomyProperties
import com.wnl.cashchat.api.domain.economy.properties.EvolutionProperties
import com.wnl.cashchat.api.domain.economy.service.WalletService
import com.wnl.cashchat.api.domain.evolution.exception.*
import com.wnl.cashchat.api.domain.evolution.persistence.entity.EvolutionAttempt
import com.wnl.cashchat.api.domain.evolution.persistence.entity.EvolutionResult
import com.wnl.cashchat.api.domain.evolution.persistence.repository.EvolutionAttemptRepository
import org.springframework.dao.DataIntegrityViolationException
import org.springframework.stereotype.Service
import org.springframework.transaction.annotation.Transactional
import kotlin.math.min

data class EvolutionMe(
    val level: Int, val exp: Long, val failStack: Int, val maxLevel: Int,
    val requiredExp: Long?, val baseSuccessRate: Double?, val finalSuccessRate: Double?,
    val canAttempt: Boolean,
)

@Service
class EvolutionService(
    private val walletService: WalletService,
    private val evolutionAttemptRepository: EvolutionAttemptRepository,
    private val walletLedgerRepository: WalletLedgerRepository,
    private val evolutionProperties: EvolutionProperties,
    private val economyProperties: EconomyProperties,
    private val evolutionRandom: EvolutionRandom,
) {
    @Transactional
    fun attempt(userId: Long, attemptKey: String, expectedLevel: Int): EvolutionAttempt {
        if (!economyProperties.evolutionEnabled) throw FeatureDisabledException("진화 기능이 일시 중지되었습니다.")
        val wallet = walletService.ensureForUpdate(userId)               // 행 락 먼저
        evolutionAttemptRepository.findByUserIdAndAttemptKey(userId, attemptKey)?.let { return it } // I14 멱등

        if (expectedLevel != wallet.evolutionLevel)
            throw EvolutionLevelMismatchException(expectedLevel, wallet.evolutionLevel)
        val policy = evolutionProperties.policyFor(wallet.evolutionLevel)
            ?: throw EvolutionMaxLevelException(wallet.evolutionLevel)
        if (wallet.evolutionExp < policy.requiredExp)
            throw EvolutionInsufficientExpException(policy.requiredExp, wallet.evolutionExp)

        val levelBefore = wallet.evolutionLevel
        val failStackBefore = wallet.evolutionFailStack
        val expBefore = wallet.evolutionExp
        val finalRate = min(1.0, policy.baseSuccessRate + failStackBefore * evolutionProperties.failStackBonus)
        val roll = evolutionRandom.roll()
        val success = roll < finalRate

        if (success) wallet.applyEvolutionSuccess() else wallet.applyEvolutionFailure(policy.failKeepRatio)

        // EXP 변동 원장(멱등 키 = evolution:$attemptKey). 성공/실패 모두 exp 감소.
        val expDelta = wallet.evolutionExp - expBefore
        walletLedgerRepository.findByIdempotencyKey("evolution:$attemptKey") ?: walletLedgerRepository.save(
            WalletLedger(
                userId = userId, type = WalletTxType.EXP_CONSUMED, delta = expDelta,
                balanceAfter = wallet.evolutionExp, referenceId = attemptKey,
                idempotencyKey = "evolution:$attemptKey",
            ),
        )

        val attempt = EvolutionAttempt(
            userId = userId, attemptKey = attemptKey,
            levelBefore = levelBefore, levelAfter = wallet.evolutionLevel,
            requiredExp = policy.requiredExp, baseSuccessRate = policy.baseSuccessRate,
            failStackBefore = failStackBefore, finalSuccessRate = finalRate, rollValue = roll,
            result = if (success) EvolutionResult.SUCCESS else EvolutionResult.FAIL,
            expAfter = wallet.evolutionExp, failStackAfter = wallet.evolutionFailStack,
            policyVersion = evolutionProperties.policyVersion,
        )
        return try {
            evolutionAttemptRepository.saveAndFlush(attempt)
        } catch (e: DataIntegrityViolationException) { // 동시 동일 key 최종 방어
            evolutionAttemptRepository.findByUserIdAndAttemptKey(userId, attemptKey) ?: throw e
        }
    }

    @Transactional(readOnly = true)
    fun me(userId: Long): EvolutionMe {
        val w = walletService.snapshot(userId)
        val policy = evolutionProperties.policyFor(w.evolutionLevel)
        val finalRate = policy?.let { min(1.0, it.baseSuccessRate + w.evolutionFailStack * evolutionProperties.failStackBonus) }
        val canAttempt = policy != null && w.evolutionExp >= policy.requiredExp
        return EvolutionMe(
            level = w.evolutionLevel, exp = w.evolutionExp, failStack = w.evolutionFailStack,
            maxLevel = evolutionProperties.maxLevel, requiredExp = policy?.requiredExp,
            baseSuccessRate = policy?.baseSuccessRate, finalSuccessRate = finalRate, canAttempt = canAttempt,
        )
    }

    @Transactional(readOnly = true)
    fun findAttempt(userId: Long, attemptId: Long): EvolutionAttempt =
        evolutionAttemptRepository.findByIdAndUserId(attemptId, userId)
            ?: throw EvolutionAttemptNotFoundException(attemptId)
}
```
구현자 노트: `WalletLedger` 생성자 시그니처는 기존 `EnergyService`/`ChatRewardSettlementService` 사용처와 동일하게 맞춘다(`balanceAfter`는 Long; exp 잔액 사용). `WalletService.snapshot`이 지갑 없을 때 throw하므로 `me`는 기존 `EconomyController.me`와 동일하게 지갑 존재 전제. 신규 사용자 대비가 필요하면 `me`에서 `ensureInitialized` 후 재조회로 대체.

- [ ] **Step 5: Run tests to verify they pass**

Run: `./gradlew test --tests "com.wnl.cashchat.api.domain.evolution.EvolutionServiceTest"`
Expected: PASS (9 시나리오).

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(evolution): add EvolutionService with idempotent secure-random attempts"
```

---

### Task 4: EvolutionController + responses + exception handler + wiring

**Files:**
- Create: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/evolution/web/request/EvolutionAttemptRequest.kt`
- Create: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/evolution/web/response/EvolutionMeResponse.kt`
- Create: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/evolution/web/response/EvolutionAttemptResponse.kt`
- Create: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/evolution/web/controller/EvolutionController.kt`
- Create: `apps/backend/src/main/kotlin/com/wnl/cashchat/api/domain/evolution/web/exception/EvolutionExceptionHandler.kt`
- Test: `apps/backend/src/test/kotlin/com/wnl/cashchat/api/domain/evolution/EvolutionControllerTest.kt` (`@WebMvcTest`)

**Interfaces:**
- Consumes: `EvolutionService`, `Authentication.principal as Long`(기존 컨트롤러 패턴), `ErrorResponse`(common).
- Produces: `POST /api/v1/evolution/attempts`(201, `Idempotency-Key` 헤더 필수, body `{expectedLevel}`), `GET /api/v1/evolution/me`(200), `GET /api/v1/evolution/attempts/{id}`(200).
- 오류 매핑: LevelMismatch→422 `EVOLUTION_LEVEL_MISMATCH`, InsufficientExp→422 `EVOLUTION_INSUFFICIENT_EXP`, MaxLevel→422 `EVOLUTION_MAX_LEVEL`, AttemptNotFound→404 `EVOLUTION_ATTEMPT_NOT_FOUND`, IdempotencyKeyRequired→400 `EVOLUTION_IDEMPOTENCY_KEY_REQUIRED`, FeatureDisabled→503 `FEATURE_DISABLED`.

- [ ] **Step 1: Write the failing test**

`EvolutionControllerTest.kt` (`@WebMvcTest(EvolutionController::class)` + `@AutoConfigureMockMvc(addFilters=false)` + `@MockBean EvolutionService`, `jwtTokenHandler`, `JpaMetamodelMappingContext`; 기존 `EconomyControllerTest` 웹슬라이스 컨벤션을 그대로 모방. principal = `UsernamePasswordAuthenticationToken(1L, null)`):
```kotlin
package com.wnl.cashchat.api.domain.evolution

// 기존 웹슬라이스 테스트와 동일한 import/셋업
class EvolutionControllerTest : FunSpec({
    test("POST attempts returns 201 with result") {
        // service.attempt(1L, "key-1", 1) 스텁 → SUCCESS attempt(id=10, levelAfter=2)
        // post /api/v1/evolution/attempts, header Idempotency-Key=key-1, body {"expectedLevel":1}
        //   → status 201, $.result == "SUCCESS", $.levelAfter == 2
    }
    test("POST attempts without Idempotency-Key returns 400 EVOLUTION_IDEMPOTENCY_KEY_REQUIRED") { }
    test("POST attempts maps EvolutionLevelMismatchException to 422 EVOLUTION_LEVEL_MISMATCH") { }
    test("POST attempts maps EvolutionInsufficientExpException to 422 EVOLUTION_INSUFFICIENT_EXP") { }
    test("POST attempts maps EvolutionMaxLevelException to 422 EVOLUTION_MAX_LEVEL") { }
    test("GET me returns 200 snapshot") { }
    test("GET attempts/{id} maps EvolutionAttemptNotFoundException to 404") { }
})
```
구현자 노트: 각 테스트를 컴파일·실행 가능하게 완성. `Idempotency-Key` 누락 케이스는 컨트롤러가 `required=false`로 받아 blank 시 `EvolutionIdempotencyKeyRequiredException`을 던지고 핸들러가 400 매핑.

- [ ] **Step 2: Run test to verify it fails**

Run: `./gradlew test --tests "com.wnl.cashchat.api.domain.evolution.EvolutionControllerTest"`
Expected: FAIL — 컨트롤러/응답/핸들러 미존재.

- [ ] **Step 3: Implement requests/responses**

`EvolutionAttemptRequest.kt`:
```kotlin
package com.wnl.cashchat.api.domain.evolution.web.request
data class EvolutionAttemptRequest(val expectedLevel: Int)
```
`EvolutionMeResponse.kt`:
```kotlin
package com.wnl.cashchat.api.domain.evolution.web.response

data class EvolutionMeResponse(
    val level: Int, val exp: Long, val failStack: Int, val maxLevel: Int,
    val requiredExp: Long?, val baseSuccessRate: Double?, val finalSuccessRate: Double?,
    val canAttempt: Boolean,
)
```
`EvolutionAttemptResponse.kt`:
```kotlin
package com.wnl.cashchat.api.domain.evolution.web.response

import com.wnl.cashchat.api.domain.evolution.persistence.entity.EvolutionAttempt
import com.wnl.cashchat.api.domain.evolution.persistence.entity.EvolutionResult
import java.time.Instant

data class EvolutionAttemptResponse(
    val id: Long, val result: EvolutionResult,
    val levelBefore: Int, val levelAfter: Int, val requiredExp: Long,
    val baseSuccessRate: Double, val failStackBefore: Int, val finalSuccessRate: Double,
    val expAfter: Long, val failStackAfter: Int, val policyVersion: Int, val createdAt: Instant?,
) {
    companion object {
        fun from(a: EvolutionAttempt) = EvolutionAttemptResponse(
            id = a.id, result = a.result, levelBefore = a.levelBefore, levelAfter = a.levelAfter,
            requiredExp = a.requiredExp, baseSuccessRate = a.baseSuccessRate,
            failStackBefore = a.failStackBefore, finalSuccessRate = a.finalSuccessRate,
            expAfter = a.expAfter, failStackAfter = a.failStackAfter,
            policyVersion = a.policyVersion, createdAt = a.createdAt,
        )
    }
}
```
구현자 노트: `BaseEntity`의 생성시각 접근자명을 확인해 `createdAt` 매핑(접근자명이 다르면 맞추고, 없으면 해당 필드 제외).

- [ ] **Step 4: Implement controller + exception handler**

`EvolutionController.kt`:
```kotlin
package com.wnl.cashchat.api.domain.evolution.web.controller

import com.wnl.cashchat.api.domain.evolution.exception.EvolutionIdempotencyKeyRequiredException
import com.wnl.cashchat.api.domain.evolution.service.EvolutionService
import com.wnl.cashchat.api.domain.evolution.web.request.EvolutionAttemptRequest
import com.wnl.cashchat.api.domain.evolution.web.response.EvolutionAttemptResponse
import com.wnl.cashchat.api.domain.evolution.web.response.EvolutionMeResponse
import org.springframework.http.HttpStatus
import org.springframework.security.core.Authentication
import org.springframework.web.bind.annotation.*

@RestController
@RequestMapping("/api/v1/evolution")
class EvolutionController(
    private val evolutionService: EvolutionService,
) {
    @GetMapping("/me")
    fun me(authentication: Authentication): EvolutionMeResponse {
        val m = evolutionService.me(authentication.userId())
        return EvolutionMeResponse(m.level, m.exp, m.failStack, m.maxLevel,
            m.requiredExp, m.baseSuccessRate, m.finalSuccessRate, m.canAttempt)
    }

    @PostMapping("/attempts")
    @ResponseStatus(HttpStatus.CREATED)
    fun attempt(
        authentication: Authentication,
        @RequestHeader(value = "Idempotency-Key", required = false) idempotencyKey: String?,
        @RequestBody request: EvolutionAttemptRequest,
    ): EvolutionAttemptResponse {
        val key = idempotencyKey?.takeIf { it.isNotBlank() } ?: throw EvolutionIdempotencyKeyRequiredException()
        val attempt = evolutionService.attempt(authentication.userId(), key, request.expectedLevel)
        return EvolutionAttemptResponse.from(attempt)
    }

    @GetMapping("/attempts/{id}")
    fun attemptById(authentication: Authentication, @PathVariable id: Long): EvolutionAttemptResponse =
        EvolutionAttemptResponse.from(evolutionService.findAttempt(authentication.userId(), id))

    private fun Authentication.userId(): Long =
        principal as? Long ?: throw IllegalArgumentException("Invalid authenticated principal")
}
```

`EvolutionExceptionHandler.kt`:
```kotlin
package com.wnl.cashchat.api.domain.evolution.web.exception

import com.wnl.cashchat.api.common.web.response.ErrorResponse
import com.wnl.cashchat.api.domain.economy.exception.FeatureDisabledException
import com.wnl.cashchat.api.domain.evolution.exception.*
import org.springframework.http.HttpStatus
import org.springframework.http.ResponseEntity
import org.springframework.web.bind.annotation.ExceptionHandler
import org.springframework.web.bind.annotation.RestControllerAdvice

@RestControllerAdvice(basePackages = ["com.wnl.cashchat.api.domain.evolution"])
class EvolutionExceptionHandler {
    @ExceptionHandler(EvolutionLevelMismatchException::class)
    fun mismatch(e: EvolutionLevelMismatchException) =
        ResponseEntity.status(HttpStatus.UNPROCESSABLE_ENTITY)
            .body(ErrorResponse("EVOLUTION_LEVEL_MISMATCH", e.message ?: "레벨 불일치"))

    @ExceptionHandler(EvolutionInsufficientExpException::class)
    fun insufficient(e: EvolutionInsufficientExpException) =
        ResponseEntity.status(HttpStatus.UNPROCESSABLE_ENTITY)
            .body(ErrorResponse("EVOLUTION_INSUFFICIENT_EXP", e.message ?: "경험치 부족"))

    @ExceptionHandler(EvolutionMaxLevelException::class)
    fun maxLevel(e: EvolutionMaxLevelException) =
        ResponseEntity.status(HttpStatus.UNPROCESSABLE_ENTITY)
            .body(ErrorResponse("EVOLUTION_MAX_LEVEL", e.message ?: "최대 레벨"))

    @ExceptionHandler(EvolutionAttemptNotFoundException::class)
    fun notFound(e: EvolutionAttemptNotFoundException) =
        ResponseEntity.status(HttpStatus.NOT_FOUND)
            .body(ErrorResponse("EVOLUTION_ATTEMPT_NOT_FOUND", e.message ?: "시도 없음"))

    @ExceptionHandler(EvolutionIdempotencyKeyRequiredException::class)
    fun keyRequired(e: EvolutionIdempotencyKeyRequiredException) =
        ResponseEntity.status(HttpStatus.BAD_REQUEST)
            .body(ErrorResponse("EVOLUTION_IDEMPOTENCY_KEY_REQUIRED", e.message ?: "Idempotency-Key 필요"))

    @ExceptionHandler(FeatureDisabledException::class)
    fun disabled(e: FeatureDisabledException) =
        ResponseEntity.status(HttpStatus.SERVICE_UNAVAILABLE)
            .body(ErrorResponse("FEATURE_DISABLED", e.message ?: "기능 중지"))
}
```
구현자 노트: `ErrorResponse` 생성자 시그니처는 기존 `EconomyExceptionHandler` 사용처와 동일하게 맞춘다(code, message 순).

- [ ] **Step 5: Run test + full suite to verify they pass**

Run: `./gradlew test --tests "com.wnl.cashchat.api.domain.evolution.EvolutionControllerTest"`
그 다음 전체: `./gradlew test`
Expected: PASS (전체 그린).

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(evolution): add evolution API (me, attempts) with error mapping"
```

---

## Self-Review (작성자 점검 완료)

- **Spec coverage:** §8 진화(성공/실패/failStack/EXP 보존), §10.9 진화 보안(서버 RNG 1회 결정·기록 항목 levelBefore/requiredExp/baseSuccessRate/failStack/finalSuccessRate/result/policyVersion/createdAt), §13 I6/I14, line 100~105 API 3종·expectedLevel mismatch·EVOLUTION_MAX_LEVEL — 모두 Task로 매핑.
- **Type consistency:** `EvolutionProperties.LevelPolicy`, `EvolutionAttempt`, `EvolutionMe`, 응답 DTO 필드명 Task 간 일치. `roll()`/`finalRate`/`policyFor` 시그니처 일관.
- **No placeholders:** 모든 코드 단계는 실제 코드 포함. 테스트 보일러플레이트(TestContainers/웹슬라이스)는 "기존 X 테스트 컨벤션 모방"으로 위임(코드베이스에 확립된 패턴이라 전체 전사 대신 참조가 적절).
- **확정 결정 4종 반영:** 선형 failStack 공식, 레벨별 보존율, Idempotency-Key 헤더, EVOLUTION_MAX_LEVEL/422.
